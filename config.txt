# Use this to get variable names and create dicts. Will be easier to convert
# them to json and retrieve.
# Layout is assumed contiguous and if elements add up to less than size, the
# remaining bytes are ignored(i.e. bytes not mapped are ignored)

[SHM1]
name = /channels_shm
size = 1024
mq_msg_id = 1
lock = 0
dict = longitude latitude altitude
layout = f f f

[SHM2]
name = /eeprom_shm
size = 1024
mq_msg_id = 2
lock = /eeprom_sem
dict = eep_test
layout = i

